import 'package:flutter/material.dart';

class ListTile extends StatelessWidget {
  const ListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 70, width: 300);
  }
}
